<html lang="en-gb">
<header>
    <%- include('_head') -%>
        <nav>
            <%- include('_header') -%>
        </nav>
</header>

<main>
    <h1>Users</h1>
    <ol>
        <% results.forEach(function(user) {%>
            <li>
                <%= user.name %>
            </li>
            <% }); %>
    </ol>
    <%if (next) { %>
        <a href="<%= next %>">Next</a>
        <% } %>
            <%if (previous) { %>
                <a href="<%= previous %>">Previous</a>
                <% } %>
                    <p> <a href="/users/create">Create User</a></p>
</main>
<footer>
    <%- include('_footer') -%>
</footer>

</html>